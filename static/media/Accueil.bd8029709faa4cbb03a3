import React from 'react';
import { useNavigate } from 'react-router-dom';

function Accueil() {
  const navigate = useNavigate();
  return (
    <div>
      Accueil
      <button onClick={() => navigate('/faq')}>Aller à la FAQ</button>
    </div>
  );
}